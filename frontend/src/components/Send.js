import React from 'react';

class Send extends React.Component{
    render(){
        return(
            <React.Fragment>
                <button onClick='#'>Show Pair's</button>
                <button onClick='#'>Send</button>
            </React.Fragment>
        )
    }
}

export default Send;